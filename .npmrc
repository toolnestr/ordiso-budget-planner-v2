# Cloudflare Pages build: @cloudflare/next-on-pages declares a peer-dependency
# range of next >=14.3.0 && <=15.5.2, but this project uses Next.js 16.
# legacy-peer-deps lets npm install proceed despite the version mismatch.
# (The adapter still builds Next 16 output correctly.)
legacy-peer-deps=true
